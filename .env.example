# Resend API key — used by src/app/api/contact/route.ts to email
# submissions from the final CTA to sohomroy1504@gmail.com.
# Get one at https://resend.com/api-keys, then copy this file to
# .env.local and fill it in. In Resend's sandbox mode (no verified
# domain), you can only send TO the email address your Resend account
# was signed up with.
RESEND_API_KEY=
